--- a/applications/pass-desktop/forge.config.ts
+++ b/applications/pass-desktop/forge.config.ts
@@ -7,6 +7,10 @@
                   }
                 : undefined,
         osxUniversal: { x64ArchFiles: 'Contents/Resources/assets/proton_pass_nm_host' },
+        // Point @electron/packager at the pre-fetched Electron zip directory
+        // so it never needs to reach github.com during the Flatpak build.
+        // The env var is only set in the Flatpak sandbox; normal builds are unaffected.
+        ...(process.env.FLATPAK_ELECTRON_ZIP_DIR ? { electronZipDir: process.env.FLATPAK_ELECTRON_ZIP_DIR } : {}),
     },
     rebuildConfig: {},
     makers: [
